# Cypress Environment Variables
# Copy this file to cypress.env.json and update with your actual credentials

# WiseAdmit Test Credentials
# IMPORTANT: Update these with your actual WiseAdmit account credentials
TEST_EMAIL=your-email@example.com
TEST_PASSWORD=your-password-here

# Note: cypress.env.json is gitignored to protect your credentials
# Never commit actual credentials to version control
